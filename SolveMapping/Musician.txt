Musician(musicanId(primaryKey),name,phone,st,city)

Instrament(name(uniqueKey),key)

musician-Instrument(id(primaryKey),instrumentName(foreign),musicianId(foreign))

Album(id(primaryKey),date,title,musicianId(foreign))

Song(title(uniqueKey),author,albumId(foreign))

musician-song(id(primaryKey),songTitle(foreign),musicianId(foreign))